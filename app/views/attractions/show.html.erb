# <%= button_to "Go on this ride", :action => "show" %>

<%= form_for @rides do |f| %>
    <% if current_user %>
    <%= f.hidden_field :user_id, value => @rides.id %>
    <%= f.hidden_field :attraction_id, value => @rides.id %>
    <% if user.admin %>
    <% p "ADMIN" %>
    <% end %>
